apiVersion: v1
kind: Service
metadata:
  name: marketplace-service
  namespace: yana-production
  labels:
    app.kubernetes.io/name: yana-os
    app.kubernetes.io/component: marketplace-service
spec:
  selector:
    app: marketplace-service
  ports:
    - name: http
      protocol: TCP
      port: 8005
      targetPort: 8005
  type: ClusterIP
