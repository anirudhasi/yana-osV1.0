apiVersion: v1
kind: Service
metadata:
  name: fleet-service
  namespace: yana-production
  labels:
    app.kubernetes.io/name: yana-os
    app.kubernetes.io/component: fleet-service
spec:
  selector:
    app: fleet-service
  ports:
    - name: http
      protocol: TCP
      port: 8003
      targetPort: 8003
  type: ClusterIP
